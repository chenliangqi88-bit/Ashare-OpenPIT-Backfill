name: Runner Smoke Test

on:
  push:
    paths:
      - '.github/workflows/runner-smoke.yml'
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: |
          echo "runner-ok"
          uname -a
          python --version
